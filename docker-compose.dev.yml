version: "3.9"

services:
  web:
    build:
      context: ./front-end
      dockerfile: Dockerfile
    ports:
      - 80:80
    volumes:
      - ./front-end/dist/:/usr/share/nginx/html/:ro
      - ./front-end/nginx/logs/:/var/log/nginx/:rw
